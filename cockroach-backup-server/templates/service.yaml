apiVersion: v1
kind: Service
metadata:
  name: {{ include "cockroach-backup-server.fullname" . }}
  labels:
    {{- include "cockroach-backup-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "cockroach-backup-server.selectorLabels" . | nindent 4 }}
